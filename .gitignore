node_modules/
dist/
site/
coverage/
*.log
.DS_Store

# Atelier de l'exécution du plan (journal, briefs, rapports, paquets de revue).
.superpowers/

# Worktrees créés par les agents.
.claude/worktrees/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
